"""第 2 层：Kernel 机制。

职责：提供能力注册与覆盖解析、turn 执行、输入分流、插件运行时、配置加载与
可观测性等机制，只依赖 contracts。
不负责：实现具体能力（模型、存储、工具、渠道），也不认识任何具体 provider。

本目录当前有 `registry/`（`D06`：能力注册与覆盖解析）与 `turn/`（`D08`：取消与预算），
其余机制由开发方案 `D09`–`D14` 逐个落地。
"""
