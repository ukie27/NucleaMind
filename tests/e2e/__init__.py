"""端到端验收：需求 §16.1 的开箱可用里程碑（`D24`）。

职责：以「一台全新机器上的用户」为视角跑完整条路径——`nm init` 生成配置、导出一个环境
变量、`nm run` 完成一次带工具调用的 turn，以及缺凭据、可中断、禁用内建三条边界。
不负责：单元级断言（各层自己的测试目录）、装配链的内部结构（`tests/runtime/`）。

**这一层与 `tests/integration/` 的分界线**：那边的 Fake 在**能力边界**上（模型、会话存储、
工具都是假的），验的是装配链装得起来；这边只有**线格式**是假的（`httpx.MockTransport`
录制的响应），模型供应商、会话存储、文件工具、命令、CLI 入口全是真的内建。因此这里失败
的东西，`tests/integration/` 一条也看不见——例如「生成的配置自己加载不了」。
"""

from __future__ import annotations
