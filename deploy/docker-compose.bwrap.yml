x-bwrap-security: &bwrap-security
  cap_add:
    - SYS_ADMIN
  security_opt:
    - apparmor=unconfined
    - seccomp=unconfined

# 服务名与 docker-compose.yml 同步（`D31` 起只剩两个）。
services:
  nucleamind-serve:
    <<: *bwrap-security

  nucleamind-cli:
    <<: *bwrap-security
